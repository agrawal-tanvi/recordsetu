import React from 'react';

// TopBar controls (Language pills, Font sizing, and Login) are unified directly
// inside Header.jsx to eliminate the raw unstyled text bar above the header.
export const TopBar = () => {
  return null;
};

export default TopBar;